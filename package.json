{
  "name": "kotoba-no-mori",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "shx mkdir -p public && shx cp -r assets css data js public && shx cp index.html public/game.html",
    "build": "vinext build",
    "postbuild": "shx mkdir -p dist/.openai && shx cp .openai/hosting.json dist/.openai/hosting.json"
  },
  "dependencies": {
    "react": "^19.2.6",
    "react-dom": "^19.2.6",
    "vinext": "^1.0.0-beta.4"
  },
  "devDependencies": {
    "@vitejs/plugin-rsc": "^0.5.26",
    "shx": "^0.4.0"
  }
}
